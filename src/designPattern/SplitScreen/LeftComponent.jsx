import React from 'react';
import PropTypes from 'prop-types';

LeftComponent.propTypes = {
    
};

function LeftComponent({children}) {
    return (
        <>
            {children}
        </>
    );
}

export default LeftComponent;