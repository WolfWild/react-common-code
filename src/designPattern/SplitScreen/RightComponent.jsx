import React from 'react';
import PropTypes from 'prop-types';

RightComponent.propTypes = {
    
};

function RightComponent({children}) {
    return (
        <>
            {children}
        </>
    );
}

export default RightComponent;